# gdc-mcp 환경변수 override (선택). 기본값은 .mcp.json에 dev 도메인으로 지정돼 있다.
# 로컬 개발/디버깅 시에만 이 파일을 .env로 복사해 localhost로 덮어쓴다(.env는 커밋 금지).
# 인증은 브라우저 핸드오프(gdc_login) 전용 — username/password 변수는 사용하지 않는다.
GDC_BASE_URL=http://localhost:8000
# 사용자에게 보여줄 태스크 링크용 프론트엔드 주소
GDC_WEB_URL=http://localhost:5173
