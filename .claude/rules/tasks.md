# Action Rules for Tasks
* **작업 수립은 영어로 진행하고 최종 결과만 한글로 작성**
* **새로운 작업 요청으로 인해 논리적인 위배가 발생하는 경우, 내용을 사용자에게 설명하고 확인 받아야 함**

# 주요 규칙
* **모든 작업전에 작업 요청 문서를 우선 작성하고 확인을 받아야함**
* **작업의 단계가 구분되어 있는 경우 단계별 진행을 마칠때 마다 작업 항목 체크표시**
* .md와 같은 문서화 작업은 요청한 것만 작업해야함
* 자료 조사는 공식 문서를 기준으로 꼼꼼하게 검토하고 반드시 출처를 명확하게 제시할 것
* 불필요한 문서화 작업은 하지 말 것
* ui 변경 사항은 askii diagram으로 시각화하여 설명할 것

# 요청 작업을 수행하는 방법 (다음의 단계로 수행)
* 작업 요청의 계획을 수립
* 계획의 검증 및 승인
* 작업 수행
  * 작업 요청의 단계를 완료하면 완료 항목에 체크표시
  * **MCP 도구/서버 동작이 관련되면 완료 전 `## 로컬 사전 검증`을 수행하고 결과를 작업 문서에 기록**

# 로컬 사전 검증 (MCP 도구/서버 동작 관련 시)
* **적용 조건**: 작업이 GDC MCP 도구/서버 동작(조회·생성·수정 등)을 **소비하거나 변경**하는 경우에만 수행한다. 순수 문서/UI/규칙 변경 등 검증할 도구 동작이 없으면 생략한다.
* **테스트 대상 고정**: 검증은 **테스트 워크스페이스 3([TEST] GDC 메인)** 에서만 수행한다. 프로젝트는 **작업 도메인에 맞는 테스트 프로젝트**를 고른다 — 이슈관리 관련 → `45 이슈관리 테스트`, WBS 관련 → `46 WBS 테스트`. 대응 도메인이 불명확하면 사용자에게 확인한다. **운영 워크스페이스/프로젝트(예: 6/31)에서는 검증하지 않는다.**
* **절차**: `set_context`로 위 테스트 컨텍스트로 전환 → 대상 도구/흐름을 실제 실행 → 기대 결과 확인.
  * 실행 세션의 MCP 서버에 해당 도구가 **미노출(구버전)** 이면, 업데이트된 로컬 코드를 직접 호출해 검증한다(`uv run python`으로 모듈 함수 호출; shim(.exe) 미사용).
* **원복 필수**: 검증용으로 생성한 임시 데이터(태스크·댓글 등)는 **반드시 삭제해 원복**하고, 검증 후 **컨텍스트를 원래 운영 값으로 복원**한다.
* **기록**: 검증 대상·결과(성공/실패·정리 완료 여부)를 작업 문서 `작업 결과` 체크리스트에 한 줄로 남긴다.

# docs 폴더 규칙
## docs/requests 작업 요청 기록
* 작업 요청 기록은 `/docs/requests/YYYY-MM/` 월별 하위 폴더에 저장
* 파일명: `YYYYMMDD-HHmmss-설명.md` (타임스탬프 + kebab-case 설명)
  * 예: `20260327-143052-panel-width-fix.md`
  * 타임스탬프는 파일 생성 시점 기준 (충돌 방지)
* 개별 작업 요청은 `docs/requests/TEMPLATE.md`의 형식을 따름
  * 상단 메타데이터 테이블 필수 (유형, 영역, 날짜, 상태, 관련)
  * 필요한 섹션만 작성 (불필요한 섹션 강제 금지)
* 작업 결과는 수행계획을 항목별로 정리
  * 수행계획을 완료한 항목은 체크 표시
  * 수행계획을 완료하지 못한 항목은 체크 표시하지 않음
* **작업 완료 시 `docs/INDEX.md`의 `## 이력` 섹션 맨 아래에 간략한 핵심 요약 한 줄 추가**
* **새 세션 시작 시 `docs/INDEX.md`를 읽어 과거 작업 맥락 파악**
  * INDEX.md에 없는 경우 `docs/requests/` 폴더를 탐색하여 최근 파일 확인

